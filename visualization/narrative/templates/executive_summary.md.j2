## Executive Summary

{{ passing_count }} of {{ total }} workflows pass all comparisons. {{ reweight_count }} need `--traffic-mix` reweighting. {{ unresolved_count }} have unresolved issues (engine problems or structural drift).

| Status | Count | Workflows |
|--------|-------|-----------|
| Passing | {{ passing_count }} | {{ passing_workflows | join(", ") if passing_workflows else "None" }} |
| Need Reweighting | {{ reweight_count }} | {{ bucket_2 | join(", ") if bucket_2 else "None" }} |
| Engine Problems | {{ bucket_1 | length }} | {{ bucket_1 | join(", ") if bucket_1 else "None" }} |
| Structural Drift | {{ bucket_3 | length }} | {{ bucket_3 | join(", ") if bucket_3 else "None" }} |

{% if unresolved_count == 0 and reweight_count == 0 -%}
All workflows are within acceptable projection accuracy. The projection engine is ready for production use.
{% elif unresolved_count == 0 and reweight_count > 0 -%}
All issues can be resolved with `--traffic-mix` reweighting. No engine changes needed.
{% else -%}
Action required: {{ unresolved_count }} workflow(s) need investigation before production deployment.
{% endif %}
